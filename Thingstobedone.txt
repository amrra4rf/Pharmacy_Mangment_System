make remove function which shifts the array to remove the removed item
Employee class
thing to be sorted (items ) according to price and name
in the remove function if the amount to be removed is = to the avilable amount remove the item from the array +print item removed sucessfully
Check all Exception Handlings 

*********continue making the login and sign up functions in the pharamacy class

make the password field in the person class
add display items for the customer




 // Create an instance of the Pharmacy
       Pharmacy pharmacy = Pharmacy.getInstance();

       // Simulate adding a doctor
       System.out.println("Adding a doctor...");
       Doctor doctor = new Doctor("John Doe", 35, 50000, "password123");
       pharmacy.person[Pharmacy.persons_count_in_pharamcy++] = doctor;

       // Simulate adding a customer
       System.out.println("Adding a customer...");
       Customer customer = new Customer("Jane Smith", 28, 'f', true, 100.0, "customerpass");
       pharmacy.person[Pharmacy.persons_count_in_pharamcy++] = customer;

       // Simulate doctor login
       System.out.println("Doctor logging in...");
       pharmacy.Dr_login(doctor.id, "password123");

       // Simulate customer login
       System.out.println("Customer logging in...");
       pharmacy.customer_login("Jane Smith", "customerpass");

       // Simulate adding items to the pharmacy
       System.out.println("Doctor adding items to the pharmacy...");
       LocalDate expiryDate = LocalDate.now().plusMonths(6);
       doctor.add_item("Aspirin", 100, expiryDate, 5.99);
       doctor.add_item("Cough Syrup", 50, expiryDate, 12.99, 'l'); // Adding a liquid item

       // Simulate customer making an order
       System.out.println("Customer making an order...");
       customer.make_order("Aspirin", 2);

       // Simulate doctor shipping an item
       System.out.println("Doctor shipping items...");
       doctor.ship_item();

       // Display the current state of the pharmacy
       System.out.println("Current state of the pharmacy:");
       for (int i = 0; i < Pharmacy.item_count_in_pharmacy; i++) {
           System.out.println("Item: " + pharmacy.items[i].name + ", Count: " + pharmacy.items[i].count);
       }